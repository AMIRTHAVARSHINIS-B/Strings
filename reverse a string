class Solution {
public:
    void reverseString(vector<char>& s) {
       reverse(s.begin(),s.end());
        int no_ele=s.size();
       for(int i=0;i<no_ele;i++)
       {
           cout<<s[i]
       }
    }  
};
